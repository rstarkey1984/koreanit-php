# 🕸️ 웹(Web)이란 무엇인가?

**웹(Web)** 은 "**월드 와이드 웹(WWW, World Wide Web)**"의 줄임말로,
인터넷을 통해 사람들이 정보를 **주고받고 공유**할 수 있는 **정보
시스템**입니다.

-   **인터넷(Internet)** : 전 세계의 컴퓨터가 연결된 네트워크
-   **웹(Web)** : 그 인터넷 위에서 작동하는 여러 서비스 중 하나
    → 이메일, 파일전송(FTP), 원격접속(SSH) 같은 다른 서비스와
    구별됩니다.

즉, 인터넷이 '도로망'이라면, 웹은 그 도로 위를 달리는 '자동차(정보)'입니다.

우리가 사용하는 **웹사이트**, **웹앱**, **유튜브**, **네이버 블로그** 등은 모두 웹의 한 형태입니다.

## 1. 웹의 기본 구조 (클라이언트-서버 모델)

웹은 **클라이언트(Client)** 와 **서버(Server)** 의 상호작용으로
작동합니다.\
이를 **클라이언트-서버 모델(Client-Server Model)** 이라고 합니다.

![클라이언트-서버 모델](https://private-user-images.githubusercontent.com/4231941/505783272-2e0029af-0464-4762-b299-3df24c256e08.png?jwt=eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9..nPlssz40KuvWGl08RcRdiPsjkKunfayqARhM1FbG7Os)

### 💻 클라이언트(Client)

-   웹 브라우저(Chrome, Edge, Safari 등)를 사용하는 **사용자** 컴퓨터 또는 소프트웨어
-   서버에 정보를 **요청(request)** 하는 역할
-   예: "구글 홈페이지를 열어줘!"

### 🖥 서버(Server)

-   웹페이지나 데이터를 **저장하고 제공**하는 컴퓨터 혹은 소프트웨어
-   클라이언트의 요청에 **응답(response)** 하여 결과를 보냄
-   예: "여기 구글 메인 HTML을 보냅니다!"


## 2. 브라우저에서 www.google.com 을 입력했을 때의 전체 흐름

### 1️⃣ URL 입력과 주소 해석

사용자가 브라우저 주소창에 `www.google.com` 을 입력하면,  
브라우저는 이것이 **도메인 이름**이라는 것을 인식합니다.

> `www.google.com` = 사람이 읽기 쉬운 이름 (도메인)  
> IP 주소 예: `142.250.206.132` (컴퓨터가 이해하는 실제 주소)

도메인이란 인터넷에서 특정 웹사이트나 서버를 구분하기 위한 이름입니다. 쉽게 말해, 인터넷 주소의 ‘이름표’ 역할을 합니다.     
즉 IP 주소는 기억하기 어렵기 때문에, 사람이 읽기 쉬운 문자 형태의 주소로 만든 것이 바로 도메인입니다.


### 2️⃣ DNS 조회 (Domain Name System)

브라우저는 이 도메인 이름이 어떤 **IP 주소**인지 알아내기 위해  
**DNS 서버**에 질의(Query)를 보냅니다.

조회 순서:

1. 브라우저(크롬,엣지,사파리 등) 캐시  
2. 운영체제(윈도우,리눅스 등) 캐시  
3. 라우터 캐시  
4. ISP(통신사)의 DNS 서버  
5. 루트 DNS 서버 → .com 서버 → google.com 네임서버

결과적으로  
```
www.google.com → 142.250.206.132
```
와 같은 IP 주소를 얻습니다.

### 3️⃣ TCP 연결 (3-way Handshake)

브라우저는 해당 IP 주소의 서버와 통신하기 위해 **TCP 연결**을 만듭니다.  
이는 다음 3단계로 이루어집니다:

```
클라이언트 → 서버 : SYN() (연결 요청)
서버 → 클라이언트 : SYN + ACK (요청 수락)
클라이언트 → 서버 : ACK (확인)
```

이 과정을 **3-way handshake**라고 합니다.
- SYN = Synchronize
- ACK = Acknowledge

### 4️⃣ HTTPS 보안 연결 (TLS Handshake)

구글은 HTTPS(보안 프로토콜)을 사용하므로,  
TCP 연결 후 **TLS(SSL) 암호화 연결**이 추가로 설정됩니다.

- 서버가 인증서(SSL Certificate)를 보냄  
- 클라이언트가 인증서 유효성 검증 (CA 서명 확인)  
- 양쪽이 암호 키를 교환해 **보안 채널** 생성

이후 통신은 모두 암호화되어 주고받습니다 🔒

### 5️⃣ HTTP 요청 전송 (Request)

이제 브라우저는 구글 서버에 실제 웹페이지를 요청합니다.

```
GET / HTTP/1.1
Host: www.google.com
User-Agent: Chrome/129.0
Accept: text/html,application/xhtml+xml
```

즉,  
> “나는 구글 홈페이지의 기본 문서(index.html)를 보고 싶어요.”

### 6️⃣ 서버의 응답 (Response)

구글 서버는 요청을 받으면 HTML 문서를 반환합니다.

```
HTTP/1.1 200 OK
Content-Type: text/html
Content-Length: 12500
```

본문에는 **HTML 코드**가 담겨 있습니다.  
브라우저는 HTML을 읽고 추가 리소스들을 요청합니다:

- CSS (`style.css`)  
- JavaScript (`main.js`)  
- 이미지 (`logo.png`)  
- 글꼴 등


### 7️⃣ 렌더링(Rendering)

브라우저는 받은 HTML을 파싱하여 DOM(Document Object Model)을 만들고,  
CSS를 적용해 스타일을 입히고 JS를 실행시켜 동적 기능을 구현합니다.

이 과정을 통해 **화면에 구글 홈페이지가 표시됩니다.**



## 요약정리
> 브라우저 주소창에 URL을 입력하는 단순한 행위는  
> 사실상 수십 개의 네트워크, 보안, 프로토콜 과정이 순식간에 일어나는 결과입니다.

단계별로 간단히 정리하자면 다음과 같습니다.

| 단계 | 이름 | 설명 |
|------|------|------|
| 1 | URL 입력 | 도메인 이름을 인식 |
| 2 | DNS 조회 | 도메인을 IP 주소로 변환 |
| 3 | TCP 연결 | 서버와 통신 채널 생성 (3-way handshake) |
| 4 | TLS 설정 | 보안 연결 (HTTPS) |
| 5 | HTTP 요청 | 서버에 페이지 요청 |
| 6 | HTTP 응답 | 서버가 HTML/CSS/JS 반환 |
| 7 | 렌더링 | 브라우저가 화면 구성 |



## 🔍 크롬 개발자 도구로 확인하기

**Network 탭**에서 다음을 직접 볼 수 있습니다:

- 첫 요청: `GET https://www.google.com/`  
- 응답: `200 OK`  
- Type: `document`  
- Size, Time, Content-Type 등  
- 그리고 연속된 여러 요청들 (script, style, image, fonts...)

